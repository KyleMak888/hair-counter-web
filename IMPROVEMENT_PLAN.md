# 毛发识别精度改进方案

## 问题分析

根据对样本图片的测试分析，当前算法存在以下问题：

### 1. 检测数量不稳定
- 同一组样本图片（3661-1-02系列）在默认配置下检测到 13-16 根
- 调整参数后检测数量在 8-19 根之间波动
- 说明算法对参数非常敏感，缺乏鲁棒性

### 2. 核心算法局限
当前算法基于 **形态学 + Otsu 阈值** 的传统 CV 方法：
- 使用 MORPH_BLACKHAT 提取深色区域
- Otsu 自动阈值分割
- 连通域分析
- 启发式簇分裂估计

**主要缺陷**：
- **光照敏感**：不同光照下阈值不稳定
- **粘连问题**：轻微接触的毛发会被合并
- **细小目标**：对比度不足的细毛容易被过滤
- **背景干扰**：阴影、污点容易误检
- **簇分裂不准确**：启发式方法无法可靠分离重叠毛发

### 3. 参数困境
- `min_contrast=35`：提高会漏检浅色毛发，降低会增加误检
- `threshold_offset=0`：调整能改变检测数量，但无法系统性提高准确率
- 参数需要针对每批图片手动调整，无法泛化

## 改进方案

### 方案 A：增强传统 CV 算法（短期，成本低）

#### A1. 多尺度检测
当前算法只在单一尺度检测，容易漏掉不同粗细的毛发。

**改进**：
- 构建图像金字塔（3个尺度）
- 在每个尺度运行检测
- 合并结果并去重（NMS）

**优点**：能检测到更多细小毛发  
**缺点**：计算量增加 2-3 倍，仍无法解决粘连问题

#### A2. 自适应局部阈值
Otsu 全局阈值对光照不均匀的图像效果差。

**改进**：
- 使用 adaptiveThreshold 或 CLAHE（对比度受限自适应直方图均衡）
- 分块处理，每块独立计算阈值
- 结合全局和局部阈值的结果

**优点**：对光照更鲁棒  
**缺点**：可能引入更多噪声，需要更复杂的后处理

#### A3. 改进簇分裂算法
当前基于梯度方向一致性的峰值检测，准确率有限。

**改进**：
- 增加形状先验（毛发通常是细长椭圆）
- 使用距离变换 + 分水岭算法分离粘连目标
- 基于面积和长宽比的更精确判断

**优点**：能更准确分离部分粘连毛发  
**缺点**：严重重叠的毛发仍无法处理

#### 预期效果
- 检测率提升：15-25%
- 误检率：可能略增
- 开发时间：2-3 天
- 成本：低

---

### 方案 B：深度学习目标检测（中长期，成本中）

使用 **YOLOv8/YOLOv11** 或 **Faster R-CNN** 进行毛发检测。

#### B1. 模型选择
推荐：**YOLOv8-nano/small**
- 速度快（10-30ms CPU）
- 小模型（6-20MB）
- 支持实例分割

#### B2. 数据需求
**最少标注量**：
- 训练集：300-500 张图片
- 验证集：50-100 张
- 测试集：50-100 张

**标注内容**：
- 每根毛发的边界框（bounding box）
- 可选：分割掩码（segmentation mask）

**标注成本**：
- 使用 LabelImg/CVAT 工具
- 每张图片标注时间：3-10 分钟（取决于毛发数量）
- 总时间：25-80 小时

#### B3. 训练流程
1. 数据准备：收集和标注样本图片
2. 数据增强：旋转、翻转、亮度调整、模糊
3. 迁移学习：使用 COCO 预训练模型
4. 训练：50-100 epochs
5. 验证和调优

**训练资源**：
- GPU：RTX 3060 或更好（可使用 Colab/Kaggle 免费 GPU）
- 训练时间：2-8 小时

#### B4. 部署
**方案 1：ONNX Runtime（推荐）**
- 导出模型为 ONNX 格式
- 使用 `onnxruntime` 推理（CPU/GPU）
- 兼容当前 Python 后端

**方案 2：OpenVINO**
- Intel CPU 优化
- 推理速度更快

**方案 3：TensorRT**
- NVIDIA GPU 加速
- 需要 GPU 服务器

#### 预期效果
- 检测率提升：50-80%
- 误检率：显著降低
- 能处理粘连、重叠的毛发
- 对光照、背景鲁棒
- 开发时间：2-4 周
- 成本：中等（主要是标注时间）

---

### 方案 C：深度学习 + 传统 CV 混合（推荐）

结合两种方法的优势。

#### 流程
1. **YOLOv8 粗检测**：快速定位所有可能的毛发区域
2. **传统 CV 精修**：对检测框内的区域进行精细分析
   - 判断是否为簇（多根粘连）
   - 使用改进的簇分裂算法
3. **后处理**：NMS 去重、边界细化

#### 优点
- 结合两种方法的优势
- DL 提供高召回率，CV 提供精确计数
- 可以先用 CV 方法快速迭代，再逐步引入 DL

#### 预期效果
- 检测率提升：60-90%
- 计数准确率最高
- 开发时间：3-5 周

---

## 推荐实施路径

### 阶段 1：快速优化（1 周）
**目标**：在不改变核心架构的前提下提升 15-25%

1. 实施方案 A1：多尺度检测
2. 实施方案 A2：自适应局部阈值
3. 增加更多可调参数到 API
4. 提供参数优化指南

**产出**：
- 改进的 `counter.py`
- 参数调优工具
- 测试报告

### 阶段 2：数据收集和标注（2-3 周）
**目标**：准备训练数据

1. 收集 500-1000 张代表性样本
2. 使用 CVAT 或 LabelImg 标注
3. 数据增强和预处理
4. 划分训练/验证/测试集

**产出**：
- 标注数据集
- 数据加载和增强脚本

### 阶段 3：训练和部署 DL 模型（2-3 周）
**目标**：部署生产可用的深度学习模型

1. 训练 YOLOv8-small 模型
2. 模型评估和调优
3. 导出为 ONNX 格式
4. 集成到后端 API
5. A/B 测试对比

**产出**：
- 训练好的 ONNX 模型
- 更新的后端代码
- 性能对比报告

### 阶段 4：混合方案优化（1-2 周）
**目标**：进一步提升计数准确率

1. 实现方案 C 的混合架构
2. 优化簇分裂算法
3. 端到端测试
4. 性能调优

**产出**：
- 最终生产系统
- 完整测试报告
- 用户文档

---

## 立即可做的改进（无需标注数据）

### 1. 参数自动优化
添加一个参数扫描工具，自动找到最佳参数组合：

```python
def find_optimal_params(image, ground_truth_count):
    best_params = None
    best_score = float('inf')
    
    for threshold_offset in range(-20, 21, 5):
        for min_contrast in range(20, 51, 5):
            config = CounterConfig(
                threshold_offset=threshold_offset,
                min_contrast=min_contrast
            )
            result = count_dark_clusters(image, config)
            score = abs(result.count - ground_truth_count)
            
            if score < best_score:
                best_score = score
                best_params = config
    
    return best_params
```

### 2. 置信度过滤
增加一个后处理步骤，过滤低置信度检测：

```python
# 只保留置信度 > 0.7 的检测
high_confidence_items = [
    item for item in result.items 
    if item.confidence > 0.7
]
```

### 3. 形态学改进
调整形态学操作的核大小和迭代次数：

```python
# 增加 open 操作去除噪声
mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, small_kernel)
# 现有的 close 操作
mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, close_kernel)
```

### 4. 多阈值投票
使用多个阈值，结果投票：

```python
def multi_threshold_detection(image):
    results = []
    for offset in [-10, 0, 10]:
        config = CounterConfig(threshold_offset=offset)
        result = count_dark_clusters(image, config)
        results.append(result)
    
    # 合并结果，使用 NMS 去重
    return merge_detections(results)
```

---

## 成本估算

| 方案 | 开发时间 | 标注成本 | 计算资源 | 预期提升 |
|------|---------|---------|---------|---------|
| A：增强 CV | 2-3 天 | ¥0 | 无需额外 | +15-25% |
| B：纯 DL | 2-4 周 | ¥5,000-15,000 | GPU 训练 | +50-80% |
| C：混合方案 | 3-5 周 | ¥5,000-15,000 | GPU 训练 | +60-90% |

**标注外包选项**：
- 国内众包平台：¥0.5-2/张
- 专业标注公司：¥2-5/张
- 自行标注：时间成本

---

## 下一步行动

### 立即执行（本周）
1. ✅ 分析当前算法性能
2. 📝 实施方案 A 的快速改进
3. 📊 在所有 28 张样本上运行测试
4. 📸 收集更多样本图片（不同光照、角度、背景）

### 短期（2 周内）
1. 决定是否投入资源做深度学习方案
2. 如果是，开始数据标注
3. 如果否，继续优化传统 CV 方法

### 需要决策
- [ ] 可接受的识别准确率目标？（例如：95% 召回率）
- [ ] 可投入的预算和时间？
- [ ] 是否有 GPU 资源用于训练？
- [ ] 能否提供人工标注的真实数据？

---

## 附录：快速测试命令

```bash
# 分析所有样本
python3 analyze_samples.py

# 生成可视化对比
python3 visualize_detection.py

# 运行现有测试
pytest tests/test_counter.py -v

# 参数扫描（待实现）
python3 optimize_params.py --input sample-images/ --output output/params.json
```
